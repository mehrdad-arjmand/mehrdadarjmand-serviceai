# Service AI - Environment Variables
# Copy this file to .env and fill in the values

# Supabase Configuration (Lovable Cloud provides these automatically)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
VITE_SUPABASE_PROJECT_ID=your_project_id

# Note: These VITE_* variables are client-side publishable keys.
# Sensitive API keys (like GOOGLE_API_KEY) are stored in Lovable's
# encrypted secrets system and accessed via Edge Functions only.
